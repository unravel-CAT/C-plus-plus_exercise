# 最低版本
cmake_minimum_required(VERSION 3.10)

# 项目名
project(C-plus-plus_exercise)

# 输出路径
set(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)

# 添加子目录
add_subdirectory(StopWatch)
add_subdirectory(BankAccount)